import { Model } from '@nozbe/watermelondb';
export class SyncOperation extends Model { static table = 'sync_operations'; }
export class SyncOperationLink extends Model { static table = 'sync_operation_links'; }
